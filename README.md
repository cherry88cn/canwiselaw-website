# CanWise Law website

Static website for CanWise Law, prepared for GitHub Pages.

The site contains the current public pages migrated from Hostinger Website Builder. It is designed to run without Hostinger runtime dependencies.

## Local build

Run `node scripts/build-site.mjs` from the repository root. The build script uses the verified local Hostinger backup and is retained as a migration record.
